# Production dependencies for the OpenManus Platform server (FastAPI API + web UI
# + Manus agent runtime). Used by the Dockerfile.
#
# This is a strict subset of requirements.txt. The packages left out
# (browsergym, gymnasium, datasets, huggingface-hub, ...) are only needed for the
# RL/benchmark tooling and are not imported anywhere under app/. browsergym alone
# transitively pulls in torch + CUDA (~4.6 GB), which is why it is excluded here.
# Version pins intentionally mirror requirements.txt.

# Core / config / models
pydantic~=2.10.6
pydantic_core~=2.27.2
pyyaml~=6.0.2
setuptools~=75.8.0

# LLM clients
openai~=1.66.3
tiktoken~=0.9.0
tenacity~=9.0.0
boto3~=1.37.18

# Web server
fastapi~=0.115.11
uvicorn~=0.34.0
python-multipart~=0.0.20

# Logging
loguru~=0.7.3
structlog>=24.1.0,<26.0.0

# Agent tools (browser, web search, crawling, MCP, sandbox client)
playwright~=1.51.0
pillow>=10.4,<11
googlesearch-python~=1.3.0
baidusearch~=1.0.3
duckduckgo_search~=7.5.3
crawl4ai~=0.6.3
beautifulsoup4~=4.13.3
requests~=2.32.3
httpx>=0.27.0
mcp~=1.5.0
docker~=7.1.0

# `uvx` is used to launch the optional Browser Use MCP server
uv>=0.6.0

# The autonomous coding loop runs `pytest -q` inside project workspaces
pytest~=8.3.5
pytest-asyncio~=0.25.3
